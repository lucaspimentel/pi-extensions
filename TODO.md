# TODO

> See also [`extensions/pi-tool-permissions/TODO.md`](extensions/pi-tool-permissions/TODO.md) for the pi-tool-permissions extension's own task list.

- [ ] Expand `extensions/plan.ts` `SAFE_BASH` allowlist with more read-only commands. Currently (~line 35) it whitelists `git status|log|diff|show|branch|remote|config --get|ls-files|blame|describe|rev-parse` plus generic `ls/pwd/cat/head/tail/wc/find/grep/rg/fd/sed -n/awk/tree/echo/which/--version` etc. Missing several harmless read-only ones the user wants: `cd` (navigate for follow-up commands), `git worktree list`, and "others" — candidates: `git stash list`, `git tag`, `git show-ref`, `git reflog`, `git submodule status`, `git diff-tree`, `git shortlog`, `stat`, `file`, `du`, `df`, `date`, `whoami`, `hostname`, `uname`, `env`/`printenv`, `sort`, `uniq`, `cut`, `column`, `jq`, `type`, `diff` (plain file diff).
	- Be careful with `cd`: confirm how the `bash` tool executes commands (fresh subprocess per call vs. persistent shell) so `cd` doesn't have surprising side effects across turns; if state doesn't persist between tool calls it's effectively a no-op/inspection-only command.
	- `git worktree` has non-read-only subcommands (`add`, `remove`, `prune`, `move`, `lock`); only allow `git worktree list` specifically, not the whole `git worktree` prefix — mirror the existing `git stash (pop|drop|clear)` split already used in `DESTRUCTIVE_BASH` (~line 24) for `git stash`.
	- After widening `SAFE_BASH`, sanity-check `DESTRUCTIVE_BASH` (~line 21) still doesn't have overlapping false negatives for any newly-allowed command (e.g. `env` can be used to run arbitrary programs like `env rm -rf .` — decide whether to allow bare `env`/`printenv` with no args only, via a stricter regex).
