# TODO

- [x] Expand `DEFAULT_AUTO_MODE.allow` in `extensions/pi-tool-permissions/index.ts` with new NL entries so the auto-mode classifier auto-allows more read-only/reversible actions out of the box (additive on top of user/project config):
	- `"Editing files in a source-controlled repository (changes are reversible via git)"` — covers `Edit` tool `no_match` prompts (e.g. "Editing a source file is not covered by the listed allow, soft deny, or hard deny rules."). Note: only affects `Edit`; `Write` has an implicit `toolDefaults["write"] = "ask"` guard (`index.ts:446`) that fires before the auto layer, so `Write` calls never reach the classifier. Covering `Write` is a separate change.
	- `"Read-only inspection commands (pwd, ls, cat, head, tail, wc, stat, file, du, df)"` — covers "Read-only inspection command that does not match any allow or deny rules."
	- `"Searching the codebase with grep, rg, find, or glob"` — covers "The command is a read-only search for a directory, not matching any allow or deny rules."
	- `"Running git status, git diff, git log, and other read-only git queries"`
	- `"Read-only GitHub API requests (e.g. fetching files, listing issues, reading repos) via gh or the web API"` — covers "The action is a read-only GitHub API request to fetch a file…"
	- `"Read-only GitHub API call to fetch a file, not sending data or modifying remote state"` — extra entry added during implementation (covers the same `gh api .../contents/...` fetch pattern more directly).
  - Updated the `allow` defaults table row in `extensions/pi-tool-permissions/README.md` to link to `DEFAULT_AUTO_MODE.allow` in `./index.ts` (and expanded the JSON example block to show all 7 entries).
  - Updated 3 assertions in `extensions/pi-tool-permissions/test-rules-and-decide.mjs` (~lines 350, 363, 375) that check the default allow list. Introduced a `DEFAULT_ALLOW` constant at the top mirroring `DEFAULT_AUTO_MODE.allow` and used `DEFAULT_ALLOW.join("|")` (plain string concatenation, not template literals) in the assertions.
  - Added a `soft_deny` entry for bulk/recursive file deletions: `Bulk or recursive file deletions (e.g. rm -rf, rm -r, Remove-Item -Recurse)` — updated README defaults table + JSON example, and 3 `soft_deny` test assertions (introduced a `DEFAULT_SOFT_DENY` constant mirroring the list).
  - Verify: `node run-all.mjs` (expect 145 passed, 0 failed, all 6 suites pass) and `npx tsc --noEmit` (clean).
- [x] Follow-ups while implementing the above:
  - Add a blank line before the classifier reason in both permission dialog titles (`Allow Bash subcommand?` and `Allow <tool>?`) — changed `\n  classifier:` to `\n\n  classifier:` in `index.ts` (~lines 1806, 1883).
  - Tweak `buildClassifierPrompt` in `index.ts` to instruct the model that the REASON should describe what the action does (e.g. 'read-only GitHub API call to fetch a file') and not mention whether it matches/fails to match rules (implied by the verdict).
- [ ] Extract pi-tool-permissions pure logic into `rules.ts` (the "extract" step after the export refactor in commit fb65b9f). `index.ts` currently exports ~45 pure/config functions that the tests import via `test-helpers.mjs` re-exporting from `./index.ts`; move them into a new `extensions/pi-tool-permissions/rules.ts` so `index.ts` imports from it and tests repoint to `./rules.ts`.
	- Functions to move (all currently `export`ed in `index.ts`): `normalizeTool`, `normalizePathSep`, `normalizeMatchPath`, `cwdGlobPattern`, `skillReadGlobs`, `piDocsReadGlobs`, `isNoopCd`, `isReadOnlyBashSubcommand`, `normalizeToolDefaultsKeys`, `coerceDefaultAction`, `compilePattern`, `parseRule`, `getMatchField`, `ruleMatches`, `inputForMatching`, `suggestRule`, `stripLineContinuations`, `splitTopLevelShell`, `stripStructuralKeywords`, `decideCompound`, `decide`, `actionIcon`, `formatBreakdownLine`, `formatBreakdown`, `recomputeBreakdown`, `modelCostScore`, `dedupeModels`, `rankClassifierModels`, `pickClassifierModel`, `describeAction`, `buildClassifierPrompt`, `parseClassifierResponse`, `verdictToAction`, `classifierCacheKey`, `classifyAction`, `mergeConfig`, `loadUserConfigRaw`, `saveUserConfig`, `userConfigPath`, `legacyUserConfigPath`, `loadProjectConfigRaw`, `saveProjectConfig`, `projectConfigPath`, `legacyProjectConfigPath`, `legacy2ProjectConfigPath`, plus constants `PROJECT_CONFIG_REL`, `LEGACY_PROJECT_CONFIG_REL`, `LEGACY2_PROJECT_CONFIG_REL`, `DEFAULT_AUTO_MODE`, `READONLY_PATH_TOOLS`, `READONLY_BASH_SAFE_ALWAYS`, `READONLY_BASH_WITH_PATHS`.
	- Keep in `index.ts`: pi-runtime glue inside `export default function` (slash-command handlers, `addRule`, prompt/steer helpers, `pwshExtraInfo`, `tildify`, `readJsonSafe`/`writeJson`, `loadConfig`, `isDefaultAction`, `isPathTool`, `consumeHeredoc`, `hasTopLevelOutputRedirect`, `tokenizeSimple`, `STATUS_KEY*`). Re-import what `rules.ts` needs from `index.ts` only if unavoidable; prefer keeping shared low-level helpers (`readJsonSafe`, `writeJson`, `dedupe`) in `rules.ts` and having `index.ts` import them.
	- Move the related interfaces/types too (`Action`, `ListAction`, `DefaultAction`, `AutoModeConfig`, `ResolvedAutoModeConfig`, `PermissionsConfig`, `ResolvedConfig`, `ParsedRule`, `SplitResult`, `SubcommandDecision`, `CompoundDecision`, `ClassifierVerdict`, `ClassifyResult`, `ClassifierComplete`, `HasAuth`) into `rules.ts` and re-import in `index.ts`.
	- `rules.ts` must have no value imports from pi (`@earendil-works/pi-coding-agent`, `@earendil-works/pi-ai`) — only `import type` for `Model`/`Api`/`Context`/`AssistantMessage` used by the classifier-model helpers. Value imports are `node:fs`, `node:crypto`, `node:os`, `node:path` only.
	- Update `test-helpers.mjs` to re-export from `./rules.ts` instead of `./index.ts`.
	- Verify: `node extensions/pi-tool-permissions/run-all.mjs` (expect all 6 suites pass, 782 passed, 0 failed) and `npx tsc --noEmit` (clean).
